components:
  schemas:
    Schema:
      type: object
      properties:
        object:
          $ref: "#/components/schemas/Object"
        same_object:
          $ref: "#/components/schemas/Object"
    Object:
      type: object
      properties:
        int:
          type: integer
        str:
          type: string
