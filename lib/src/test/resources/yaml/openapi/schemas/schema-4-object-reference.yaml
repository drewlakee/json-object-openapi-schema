components:
  schemas:
    Schema:
      type: object
      properties:
        object_array:
          type: array
          items:
            $ref: "#/components/schemas/ObjectArray"
    ObjectArray:
      type: object
      properties:
        a:
          type: integer
        b:
          type: string
